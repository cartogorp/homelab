services:
  homarr:
    container_name: homarr
    image: ghcr.io/homarr-labs/homarr:latest
    restart: unless-stopped

    volumes:
      - /srv/appdata/homarr:/appdata

    environment:
      - SECRET_ENCRYPTION_KEY={$SECRET_ENCRYPTION_KEY}

    ports:
      - "127.0.0.1:7575:7575"
